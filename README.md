# Highlight Harvester

Upload a PDF with highlight annotations, pick a page range, and download a structured Word document grouping the highlights under the source document's chapter/section headings.

## Run

```
pip install -r requirements.txt
streamlit run app.py
```

The UI opens at <http://localhost:8501>.
